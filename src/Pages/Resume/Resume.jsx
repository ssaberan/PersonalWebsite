import React from "react";
import { Helmet } from "react-helmet";

const Resume = () => {
   return (
      <>
         <Helmet>
            <title>Resume</title>
         </Helmet>
      </>
   );
};

export default Resume;
